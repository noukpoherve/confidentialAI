services:
  mongo:
    image: mongo:7
    container_name: confidential-agent-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: confidential_agent
    volumes:
      - mongo_data:/data/db
      - ../mongodb/init.js:/docker-entrypoint-initdb.d/init.js:ro

volumes:
  mongo_data:
